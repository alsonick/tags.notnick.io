export const phonkTags = (artist: string, title: string): string => {
  return `${artist},${title},${artist} ${title},${title} ${artist},${title} phonk,${artist} ${title} phonk,${title} ${artist} phonk,${artist} phonk,brazilian phonk,funk,new phonk,tiktok phonk,hard phonk,${title} funk,${artist} funk,${artist} ${title} funk`;
};
