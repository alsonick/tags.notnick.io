export const nightcoreSpedUpTags = (artist: string, title: string): string => {
  return `${artist},${title},${title} nightcore,${title} sped up,${title} sped up ${artist},${artist} ${title},${artist} ${title} sped up,${artist} nightcore,${artist} sped up,nightcore`;
};
