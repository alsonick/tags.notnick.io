export const phonkTags = (artist: string, title: string): string => {
  return `${artist},${title},${artist} ${title},${title} ${artist},${title} phonk,${artist} ${title} phonk,${title} ${artist} phonk,${artist} phonk`;
};
